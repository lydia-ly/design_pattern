import { test, expect, describe, bench } from "vitest";

describe("test class", () => {
  test("test one", () => {
    expect(1).toBe(1);
  });
  test("test two", () => {
    expect(2).toBe(2);
  });
});
// test("observer test", () => {
//   expect(1).toBe(1);
// });
